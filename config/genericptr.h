#ifndef CONFIG_GENERICPTR_H
#define CONFIG_GENERICPTR_H

#define GENERICPTR char *

/* GENERICPTR is a pointer-to-memory type, for use with ioctl. */
/* char * is safe with all C compilers. void * is safe only with ANSI */
/* but may produce fewer warnings under an ANSI compiler. */

#endif
