#ifndef CONFIG_PTYDIR_H
#define CONFIG_PTYDIR_H

#define PTYDIR "/usr/local/etc/pty"

/* PTYDIR is where pty will store its communications files. */
/* It must be owned by the same uid that pty runs as, mode 0700. */

#endif
